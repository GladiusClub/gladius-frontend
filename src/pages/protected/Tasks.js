import React from "react";

import Typography from "components/Typography";

const Tasks = () => {
  return (
    <div>
      <Typography className="text-center text-xl">Tasks</Typography>
    </div>
  );
};

export default Tasks;
